import { Injectable } from '@nestjs/common'
import { InjectModel } from '@nestjs/mongoose'
import { Model } from 'mongoose'
import { AbstractServiceSchema } from '~/_common/_abstracts/abstract.service.schema'
import { {{ pascalCase name }} } from './_schemas/{{ kebabCase name }}.schema'

@Injectable()
export class {{ pascalCase (plural name) }}Service extends AbstractServiceSchema<{{ pascalCase name }}> {
  constructor(@InjectModel({{ pascalCase name }}.name) protected readonly _model: Model<{{ pascalCase name }}, {}, {}, {}, {{ pascalCase name }}Document>) {
    super()
  }
}
